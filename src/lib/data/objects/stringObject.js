import { string } from '../../colors';

const stringObject = {
  id: 'string',
  name: 'String Object',
  description:
    'The string primitive contains methods that help with parsing, modifying and iterating through the values in a string.',
  color: `${string}`,
};

export default stringObject;
