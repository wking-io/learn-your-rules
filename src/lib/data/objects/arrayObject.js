import { array } from '../../helpers/colors';

const arrayObject = {
  id: 'array',
  name: 'Array Object',
  description:
    'The array primitive contains methods that help with accessing, modifying and iterating through the values in an array.',
  color: `${array}`,
};

export default arrayObject;
